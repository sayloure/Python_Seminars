"""
Винни-Пух попросил Вас посмотреть, есть ли в его стихах ритм. Поскольку разобраться
в его кричалках не настолько просто, насколько легко он их придумывает,
Вам стоит написать программу. Винни-Пух считает, что ритм есть, если число слогов
(т.е. число гласных букв) в каждой фразе стихотворения одинаковое.
Фраза может состоять из одного слова, если во фразе несколько слов, 
то они разделяются дефисами. Фразы отделяются друг от друга пробелами. 
Стихотворение  Винни-Пух вбивает в программу с клавиатуры. В ответе напишите “Парам пам-пам”,
если с ритмом все в порядке и “Пам парам”, если с ритмом все не в порядке

*Пример:*

**Ввод:** пара-ра-рам рам-пам-папам па-ра-па-да    
**Вывод:** Парам пам-пам  
"""


def pam(a):
    count = 0
    for i in range(len(lst)):
        if lst[i] == "а":
            count += 1
    return count


def vivod(text):
    if pam(lst) % 2 == 0:
        print("Парам пам-пам")
    else:
        print("Пам парам")


lst = list(input('Введите стихотворение: '))
vivod(lst)


# Chat GPT
# def count_vowels(phrase):
#     vowels = "AEIOUaeiou"
#     count = 0
#     for char in phrase:
#         if char in vowels:
#             count += 1
#     return count

# def check_rhythm(poem):
#     phrases = poem.split()
#     rhythm = count_vowels(phrases[0])
    
#     for phrase in phrases:
#         if count_vowels(phrase) != rhythm:
#             return "Пам парам"
    
#     return "Парам пам-пам"

# # Ввод стихотворения
# poem = input("Введите стихотворение: ")

# # Проверка ритма
# result = check_rhythm(poem)
# print(result)
